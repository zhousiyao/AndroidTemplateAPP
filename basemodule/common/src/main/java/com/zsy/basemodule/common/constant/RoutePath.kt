package com.zsy.basemodule.common.constant

interface RoutePath {
    companion object {
        // main
        const val MAIN_ACTIVITY = "/main/MainActivity"
        const val USER_INFO_FRAGMENT = "/main/UserInfoFragment"

        // common
        const val SIMPLE_PAGE_ACTIVITY = "/common/SimplePageActivity"
    }
}