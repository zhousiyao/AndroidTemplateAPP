<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zsy.app">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidTemplateAPP">
        <activity android:name=".test.activity.Task01Activity"></activity>
        <activity
            android:name=".test.activity.Instance02Activity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".test.activity.Instance01Activity"
            android:launchMode="singleInstance" />
        <activity android:name=".test.view.ViewTestActivity" />
        <activity android:name=".test.BitmapMemoryManageActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <provider
            android:authorities="aaa"
            android:name=".test.contentprovider.Test01ContentProvider"/>
    </application>

</manifest>